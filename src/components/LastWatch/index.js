import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const LastWatch = () => {
    return (
        <View style={{
            flex: 1,
            alignItems: 'center',
            justifyContent: 'center'
        }}>
            <Text>LastWatch Comment</Text>
        </View>
    )
}

export default LastWatch

const styles = StyleSheet.create({})
