import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Received = () => {
    return (
        <View style={{
            flex: 1,
            alignItems: 'center',
            justifyContent: 'center'
        }}>
            <Text>Received Comment</Text>
        </View>
    )
}

export default Received

const styles = StyleSheet.create({})
